{
  "name": "sentia.bs.startapp",
  "version": "2.0.0",
  "scripts": {
    "clean": "bsb -clean-world",
    "build": "bsb -make-world",
    "watch": "bsb -make-world -w",
    "pretest": "npm run build",
    "test": "tape lib/js/test/*.js | tap-spec"
  },
  "keywords": [
    "BuckleScript"
  ],
  "license": "MIT",
  "devDependencies": {
    "bs-platform": "1.9.0",
    "sentia.bs.tape": "^0.2.0",
    "tap-spec": "^4.1.1"
  },
  "dependencies": {
    "sentia.bs.promise": "^0.2.0"
  }
}
